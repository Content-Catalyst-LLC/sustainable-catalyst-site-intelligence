# Install and Test Site Intelligence v3.23.6.4

Run the supplied macOS installer with the flat release bundle. The installer validates checksums, creates an isolated Python environment, runs the complete test inventory twice, requires Chromium for the complete-shell and long-page WordPress embed gates, promotes GitHub and Render, verifies the live embed-isolation endpoint, and then prints the WordPress ZIP path.
