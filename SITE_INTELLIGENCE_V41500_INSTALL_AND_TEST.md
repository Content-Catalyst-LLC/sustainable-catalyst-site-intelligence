# Install and Test — Site Intelligence v4.16.0

Use the release bundle with `deploy_and_validate_site_intelligence_v4_15_0_macos.sh`. The installer validates bundle checksums, extracts the frozen repository, runs deterministic validation twice, promotes the exact Git tree, and verifies live Render parity before the WordPress plugin is considered safe to install.
