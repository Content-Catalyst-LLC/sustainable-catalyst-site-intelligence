# Site Intelligence v4.28.0 — Install and Test

Run the macOS installer with the v4.28.0 release bundle. The installer verifies bundle checksums, creates an isolated Python environment, executes deterministic validation passes, promotes GitHub/Render, and only then prints the WordPress ZIP path.

Do not install the WordPress plugin until the Render release gate verifies version 4.28.0, release id site-intelligence-v4.28.0, expected Git commit, inherited deep gates, and the Energy Systems deep gate.
