# Install and Test Site Intelligence v3.23.6.4

Run the supplied macOS installer with the flat release bundle. The installer verifies checksums, creates an isolated Python environment, runs deterministic validation twice, requires Chromium for the complete-shell, WordPress embed, and production-soak gates, promotes GitHub and Render, verifies the live startup-stability contract, and then prints the WordPress ZIP path.
