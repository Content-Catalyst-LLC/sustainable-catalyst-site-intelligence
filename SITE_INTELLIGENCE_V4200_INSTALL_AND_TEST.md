# Site Intelligence v4.13.0 — Install and Test

Run the macOS release installer against the v4.13.0 release bundle. The installer verifies SHA-256 package checksums, creates an isolated Python environment, runs deterministic repository validation, promotes the exact Git tree, and verifies the live Render release before printing the WordPress ZIP path.
