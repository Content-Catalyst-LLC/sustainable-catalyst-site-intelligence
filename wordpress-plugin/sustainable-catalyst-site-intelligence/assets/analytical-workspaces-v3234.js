(()=>{"use strict";
const VERSION="4.6.0";const q=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
function insideApp(){return Boolean(q("#app[data-scsi-release]"));}
async function api(path){const r=await fetch(path,{headers:{Accept:"application/json"},cache:"no-store"});if(!r.ok)throw new Error(`${r.status}`);return r.json();}
function routeOpen(route){const button=q(`.nav-item[data-route="${CSS.escape(route)}"]`);if(button){button.click();button.focus({preventScroll:true});return true}const u=new URL(location.href);u.searchParams.set("view",route);location.assign(u);return false}
function card(w){return `<article class="scsi-workflow-card" data-workflow-id="${esc(w.workflow_id)}"><p class="eyebrow">${esc(w.status)} · ${esc(w.route)}</p><h3>${esc(w.label)}</h3><p>${esc(w.purpose)}</p><div class="scsi-workflow-meta"><span class="scsi-workflow-chip">${w.stages.length} stages</span><span class="scsi-workflow-chip">${w.outputs.length} outputs</span><span class="scsi-workflow-chip">5 states</span><span class="scsi-workflow-chip">export</span></div><p><strong>Empty:</strong> ${esc(w.empty_state)}</p><p><strong>Degraded:</strong> ${esc(w.degraded_state)}</p><div class="scsi-workflow-actions"><button type="button" class="earth-primary-button" data-open-route="${esc(w.route)}">Open workflow</button><a class="ghost-button" href="${esc(w.deep_link)}">Open direct link</a></div></article>`}
function install(payload){if(q("#analyticalWorkflowPanel"))return;const top=q(".topbar-controls")||q(".topbar");if(!top)return;const toggle=document.createElement("button");toggle.type="button";toggle.id="analyticalWorkflowToggle";toggle.className="ghost-button scsi-workflow-toggle";toggle.setAttribute("aria-expanded","false");toggle.setAttribute("aria-controls","analyticalWorkflowPanel");toggle.innerHTML='<span aria-hidden="true">◎</span><span>Core workflows</span>';top.append(toggle);const panel=document.createElement("aside");panel.id="analyticalWorkflowPanel";panel.className="scsi-workflow-panel";panel.hidden=true;panel.setAttribute("aria-label","Core analytical workflow completion");panel.innerHTML=`<div class="scsi-workflow-head"><div><p class="eyebrow">ANALYTICAL WORKSPACE COMPLETION · v${esc(payload.version)}</p><h2>Five complete public workflows</h2><p>Ready, empty, degraded, unavailable, and export boundaries remain explicit.</p></div><button type="button" class="ghost-button" data-workflow-close>Close</button></div><div class="scsi-workflow-summary"><div><span>Operational</span><strong>${payload.summary.operational}</strong></div><div><span>Exports declared</span><strong>${payload.workflows.filter(w=>w.export_endpoint).length}</strong></div><div><span>Unsupported</span><strong>${payload.summary.unavailable}</strong></div></div><div class="scsi-workflow-grid">${payload.workflows.map(card).join("")}</div><p class="scsi-workflow-boundary">${esc(payload.completion_gate.principle)}</p>`;document.body.append(panel);const close=()=>{panel.hidden=true;toggle.setAttribute("aria-expanded","false")};toggle.addEventListener("click",()=>{panel.hidden=!panel.hidden;toggle.setAttribute("aria-expanded",String(!panel.hidden));if(!panel.hidden)q("[data-workflow-close]",panel)?.focus()});q("[data-workflow-close]",panel)?.addEventListener("click",close);panel.addEventListener("click",e=>{const b=e.target.closest("[data-open-route]");if(!b)return;close();routeOpen(b.dataset.openRoute)});document.addEventListener("keydown",e=>{if(e.key==="Escape"&&!panel.hidden){close();toggle.focus()}});window.dispatchEvent(new CustomEvent("scsi:analytical-workspaces-ready",{detail:{version:VERSION,count:payload.workflow_count}}));}
async function start(){if(!insideApp())return;try{install(await api("/public/workflows/analytical"))}catch(e){window.dispatchEvent(new CustomEvent("scsi:analytical-workspaces-error",{detail:{version:VERSION,message:String(e)}}))}}
window.SCSIAnalyticalWorkspacesV3234={version:VERSION,start,open:()=>q("#analyticalWorkflowToggle")?.click(),insideApp};if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",start,{once:true});else start();})();
