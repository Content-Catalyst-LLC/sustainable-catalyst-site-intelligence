(()=>{"use strict";
const VERSION="4.39.0",API=window.SC_SITE_INTELLIGENCE_API||location.origin,q=s=>document.querySelector(s);let catalog=null,last=null;
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
async function get(path){const r=await fetch(API+path,{headers:{Accept:"application/json"}});if(!r.ok)throw new Error(`Marine pollution ${r.status}`);return r.json()}
function ensureShell(){if(!document.querySelector('link[data-marinepollution41300]')){const l=document.createElement('link');l.rel='stylesheet';l.href='/app/assets/marine-pollution-v41300.css?v=4.39.0';l.dataset.marinepollution41300='1';document.head.appendChild(l)}const human=q('#marineHumanActivityPanel');if(!human)return;const actions=human.querySelector('.mh41200-actions');if(actions&&!q('#mhPollutionEnter')){const b=document.createElement('button');b.id='mhPollutionEnter';b.className='earth-primary-button';b.type='button';b.textContent='Pollution & water quality';actions.appendChild(b)}if(!q('#marinePollutionPanel')){const p=document.createElement('section');p.id='marinePollutionPanel';p.className='mp41300-panel';p.hidden=true;p.dataset.scsiMarinePollutionContract='marine-pollution-debris-water-quality';human.insertAdjacentElement('afterend',p)}}
function build(){const p=q('#marinePollutionPanel');if(!p||p.dataset.built)return;p.innerHTML=`<div class="mp41300-head"><div><p class="eyebrow">MARINE POLLUTION, DEBRIS &amp; WATER-QUALITY INTELLIGENCE</p><h3>Read pollution evidence without turning sparse measurements into clean-water, health, harm, or compliance claims</h3><p>Keep microplastics, litter, contaminants, discrete samples, non-detects, model analyses, forecasts, units, matrices, methods, and quality flags distinct.</p></div><div class="mp41300-actions"><button id="mpBack" class="ghost-button" type="button">Back to human activity</button><button id="mpManifest" class="ghost-button" type="button">Evidence manifest</button></div></div>
<div class="mp41300-controls"><label>Source <select id="mpSource"></select></label><label>Indicator <select id="mpIndicator"></select></label><label>Date <input id="mpDate" type="date"></label><label>Latitude <input id="mpLat" type="number" min="-90" max="90" step="0.01"></label><label>Longitude <input id="mpLon" type="number" min="-180" max="180" step="0.01"></label><button id="mpOpenSource" class="earth-primary-button" type="button">Open source</button></div>
<div class="mp41300-stage" role="img" aria-label="Marine pollution evidence orientation; no health, ecological harm, or compliance finding is inferred"><div class="mp41300-node mp41300-plastic">MICROPLASTICS</div><div class="mp41300-node mp41300-litter">MARINE LITTER</div><div class="mp41300-node mp41300-chem">CONTAMINANTS</div><div class="mp41300-node mp41300-quality">WATER QUALITY</div><div class="mp41300-center"><span>EVIDENCE ORIENTATION · NOT A HEALTH OR COMPLIANCE FINDING</span><strong id="mpStageTitle">Marine pollution evidence</strong><small id="mpStageState">No measurement, debris, threshold, health, or regulatory record loaded</small></div></div>
<div class="mp41300-evidence"><article><p class="eyebrow">EVIDENCE QUERY</p><h3 id="mpStateTitle">Preparing</h3><div id="mpMetadata"></div></article><article><p class="eyebrow">DATA TRUTH</p><h3>What this view refuses to infer</h3><div id="mpTruth"></div></article></div>`;p.dataset.built='1';q('#mpBack').addEventListener('click',leave);q('#mpManifest').addEventListener('click',download);['mpSource','mpIndicator','mpDate','mpLat','mpLon'].forEach(id=>q('#'+id).addEventListener('change',refresh));q('#mpOpenSource').addEventListener('click',()=>{const s=catalog?.sources.find(x=>x.id===q('#mpSource').value);if(s?.url)window.open(s.url,'_blank','noopener,noreferrer')})}
function option(row){return `<option value="${esc(row.id)}">${esc(row.title)}</option>`}
async function load(){catalog=await get('/public/marine-pollution/catalog');q('#mpSource').innerHTML=catalog.sources.map(option).join('');q('#mpIndicator').innerHTML=catalog.indicator_types.map(option).join('');const sp=new URLSearchParams(location.search);if(sp.get('mpSource')&&catalog.sources.some(x=>x.id===sp.get('mpSource')))q('#mpSource').value=sp.get('mpSource');if(sp.get('mpIndicator')&&catalog.indicator_types.some(x=>x.id===sp.get('mpIndicator')))q('#mpIndicator').value=sp.get('mpIndicator');for(const [id,key] of [['mpDate','mpDate'],['mpLat','mpLat'],['mpLon','mpLon']])if(sp.get(key)!==null)q('#'+id).value=sp.get(key);await refresh()}
async function refresh(){if(!catalog)return;const source=q('#mpSource').value||'noaa-ncei-marine-microplastics',indicator_type=q('#mpIndicator').value||'microplastics',date=q('#mpDate').value,lat=q('#mpLat').value,lon=q('#mpLon').value,sp=new URLSearchParams({source,indicator_type,date});if(lat!==''&&lon!==''){sp.set('latitude',lat);sp.set('longitude',lon)}const d=await get('/public/marine-pollution/state?'+sp.toString());last=d;q('#mpStageTitle').textContent=d.indicator_type.title;q('#mpStageState').textContent=`${d.source.title} · no pollution measurement or debris evidence loaded`;q('#mpStateTitle').textContent=d.source.title;q('#mpMetadata').innerHTML=`<dl><dt>Indicator</dt><dd>${esc(d.indicator_type.title)}</dd><dt>Source supports indicator</dt><dd>${d.source_supports_indicator_type?'Registered':'Not registered'}</dd><dt>Query point</dt><dd>${d.query_point?`${esc(d.query_point.latitude)}, ${esc(d.query_point.longitude)}`:'Not selected'}</dd><dt>Date</dt><dd>${esc(d.date||'Not selected')}</dd></dl><p class="mp41300-note">${esc(d.source.limitations)}</p>`;q('#mpTruth').innerHTML=`<dl><dt>Measurement</dt><dd>Not loaded</dd><dt>Debris record</dt><dd>Not loaded</dd><dt>Non-detect</dt><dd>Not loaded</dd><dt>Zero records = clean water</dt><dd>No</dd><dt>Non-detect = zero</dt><dd>No</dd><dt>Model = in-situ sample</dt><dd>No</dd><dt>Debris = source attribution</dt><dd>No</dd><dt>Health-risk finding</dt><dd>None</dd><dt>Ecological-harm finding</dt><dd>None</dd><dt>Compliance finding</dt><dd>None</dd></dl>`;const p=new URLSearchParams(location.search);p.set('mpSource',d.source.id);p.set('mpIndicator',d.indicator_type.id);if(d.date)p.set('mpDate',d.date);else p.delete('mpDate');if(d.query_point){p.set('mpLat',d.query_point.latitude);p.set('mpLon',d.query_point.longitude)}else{p.delete('mpLat');p.delete('mpLon')}history.replaceState(null,'','?'+p)}
async function enter(){ensureShell();build();q('#marinePollutionPanel').hidden=false;await load();q('#marinePollutionPanel').scrollIntoView({block:'nearest',behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});window.dispatchEvent(new CustomEvent('scsi:marine-pollution-enter',{detail:{version:VERSION}}))}
function leave(){q('#marinePollutionPanel').hidden=true;q('#marineHumanActivityPanel')?.scrollIntoView({block:'nearest',behavior:'auto'})}
async function download(){if(!last)return;const sp=new URLSearchParams({source:last.source.id,indicator_type:last.indicator_type.id,date:last.date||''});if(last.query_point){sp.set('latitude',last.query_point.latitude);sp.set('longitude',last.query_point.longitude)}const d=await get('/public/marine-pollution/export-manifest?'+sp.toString()),b=new Blob([JSON.stringify(d,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(b);a.download=`site-intelligence-marine-pollution-${last.source.id}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
function loadCoastalChange(){if(document.querySelector('script[data-coastalchange41400]'))return;const s=document.createElement('script');s.src='/app/assets/coastal-change-v41400.js?v=4.39.0';s.defer=true;s.dataset.coastalchange41400='1';document.head.appendChild(s)}function bind(){ensureShell();build();q('#mhPollutionEnter')?.addEventListener('click',enter);loadCoastalChange()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();window.SCSIMarinePollutionV41300={version:VERSION,enter,leave,refresh};
})();
