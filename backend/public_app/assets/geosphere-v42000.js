(()=>{"use strict";const VERSION="4.35.11",q=(s,r=document)=>r.querySelector(s),esc=v=>String(v??'').replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[c]));let catalog=null,last=null;async function get(u){const r=await fetch(u,{cache:'no-store'});if(!r.ok)throw new Error(`${u}: ${r.status}`);return r.json()}function ensureShell(){if(!document.querySelector('link[data-geosphere42000]')){const l=document.createElement('link');l.rel='stylesheet';l.href='/app/assets/geosphere-v42000.css?v=4.35.11';l.dataset.geosphere42000='1';document.head.appendChild(l)}const prev=q('#terrestrialEcosystemsPanel');if(!prev)return;const actions=prev.querySelector('.te41900-actions');if(actions&&!q('#teGeosphereEnter')){const b=document.createElement('button');b.id='teGeosphereEnter';b.className='earth-primary-button';b.type='button';b.textContent='Geosphere, earthquakes & volcanoes';actions.appendChild(b)}if(!q('#geospherePanel')){const p=document.createElement('section');p.id='geospherePanel';p.className='ge42000-panel';p.hidden=true;p.dataset.scsiGeosphereContract='global-geosphere-earthquake-volcano-intelligence';prev.insertAdjacentElement('afterend',p)}}function build(){const p=q('#geospherePanel');if(!p||p.dataset.built)return;p.dataset.built='1';p.innerHTML=`<div class="ge42000-shell"><div class="ge42000-kicker">EARTH OBSERVATION · SOLID EARTH</div><h2>Global geosphere, earthquake & volcano intelligence</h2><p class="ge42000-warning">SOLID-EARTH EVIDENCE · NOT AN EMERGENCY, DAMAGE OR HAZARD DETERMINATION</p><div class="ge42000-controls"><label>Source<select id="geSource"></select></label><label>Indicator<select id="geIndicator"></select></label><label>Date<input id="geDate" type="date"></label><label>Latitude<input id="geLat" type="number" step="any"></label><label>Longitude<input id="geLon" type="number" step="any"></label></div><div class="ge42000-actions"><button id="geRefresh" type="button">Refresh orientation</button><button id="geExport" type="button">Export manifest</button><button id="geBack" type="button">Back to terrestrial systems</button></div><div class="ge42000-grid"><section><h3 id="geStageTitle">Solid-Earth orientation</h3><p id="geStageState">No solid-Earth evidence record loaded</p><div id="geMetadata"></div></section><section><h3>Truth boundary</h3><div id="geTruth"></div></section></div></div>`;q('#geRefresh').addEventListener('click',refresh);q('#geExport').addEventListener('click',download);q('#geBack').addEventListener('click',leave)}function option(row){return `<option value="${esc(row.id)}">${esc(row.title)}</option>`}async function load(){catalog=await get('/public/geosphere/catalog');q('#geSource').innerHTML=catalog.sources.map(option).join('');q('#geIndicator').innerHTML=catalog.indicator_types.map(option).join('');const sp=new URLSearchParams(location.search);if(sp.get('geSource')&&catalog.sources.some(x=>x.id===sp.get('geSource')))q('#geSource').value=sp.get('geSource');if(sp.get('geIndicator')&&catalog.indicator_types.some(x=>x.id===sp.get('geIndicator')))q('#geIndicator').value=sp.get('geIndicator');for(const [id,key] of [['geDate','geDate'],['geLat','geLat'],['geLon','geLon']])if(sp.get(key)!==null)q('#'+id).value=sp.get(key);await refresh()}async function refresh(){if(!catalog)return;const source=q('#geSource').value||'usgs-earthquake-catalog',indicator_type=q('#geIndicator').value||'earthquake-event',date=q('#geDate').value,lat=q('#geLat').value,lon=q('#geLon').value,sp=new URLSearchParams({source,indicator_type,date});if(lat!==''&&lon!==''){sp.set('latitude',lat);sp.set('longitude',lon)}const d=await get('/public/geosphere/state?'+sp.toString());last=d;q('#geStageTitle').textContent=d.indicator_type.title;q('#geStageState').textContent=`${d.source.title} · no solid-Earth evidence record loaded`;q('#geMetadata').innerHTML=`<dl><dt>Indicator</dt><dd>${esc(d.indicator_type.title)}</dd><dt>Source supports indicator</dt><dd>${d.source_supports_indicator_type?'Registered':'Not registered'}</dd><dt>Query point</dt><dd>${d.query_point?`${esc(d.query_point.latitude)}, ${esc(d.query_point.longitude)}`:'Not selected'}</dd><dt>Date</dt><dd>${esc(d.date||'Not selected')}</dd></dl><p class="ge42000-note">${esc(d.source.limitations)}</p>`;q('#geTruth').innerHTML=`<dl><dt>Earthquake event</dt><dd>Not loaded</dd><dt>ShakeMap</dt><dd>Not loaded</dd><dt>Volcano notice</dt><dd>Not loaded</dd><dt>Deformation product</dt><dd>Not loaded</dd><dt>Catalog event = warning</dt><dd>No</dd><dt>ShakeMap = structural damage</dt><dd>No</dd><dt>PAGER = confirmed loss</dt><dd>No</dd><dt>Platform volcano alert</dt><dd>Not issued</dd><dt>InSAR = vertical displacement</dt><dd>No</dd><dt>InSAR = damage</dt><dd>No</dd><dt>Zero records = no hazard</dt><dd>No</dd></dl>`;const p=new URLSearchParams(location.search);p.set('geSource',d.source.id);p.set('geIndicator',d.indicator_type.id);if(d.date)p.set('geDate',d.date);else p.delete('geDate');if(d.query_point){p.set('geLat',d.query_point.latitude);p.set('geLon',d.query_point.longitude)}else{p.delete('geLat');p.delete('geLon')}history.replaceState(null,'','?'+p)}async function enter(){ensureShell();build();q('#geospherePanel').hidden=false;await load();q('#geospherePanel').scrollIntoView({block:'nearest',behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});window.dispatchEvent(new CustomEvent('scsi:geosphere-enter',{detail:{version:VERSION}}))}function leave(){q('#geospherePanel').hidden=true;q('#terrestrialEcosystemsPanel')?.scrollIntoView({block:'nearest',behavior:'auto'})}async function download(){if(!last)return;const sp=new URLSearchParams({source:last.source.id,indicator_type:last.indicator_type.id,date:last.date||''});if(last.query_point){sp.set('latitude',last.query_point.latitude);sp.set('longitude',last.query_point.longitude)}const d=await get('/public/geosphere/export-manifest?'+sp.toString()),b=new Blob([JSON.stringify(d,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(b);a.download=`site-intelligence-geosphere-${last.source.id}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}function loadSoils(){if(document.querySelector('script[data-soils42100]'))return;const s=document.createElement('script');s.src='/app/assets/soils-land-v42100.js?v=4.35.11';s.defer=true;s.dataset.soils42100='1';document.head.appendChild(s)}function bind(){ensureShell();build();q('#teGeosphereEnter')?.addEventListener('click',enter);loadSoils()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();window.SCSIGeosphereV42000={version:VERSION,enter,leave,refresh};})();
