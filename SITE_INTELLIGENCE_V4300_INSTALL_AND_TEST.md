# Site Intelligence v4.13.0 — Install and Test

The repository verifier is `verify_site_intelligence_v4_3_0_macos.sh`. It checks the v4.3 astronomical contract, immutable manifest, Python compilation, JSON/GeoJSON parsing, JavaScript syntax, WordPress PHP syntax when PHP is available, the static secret scan, and the complete regression suite. Set `SC_SI_RUN_BROWSER_GATE=1` to run the focused astronomical Chromium gate in a suitable build environment.
